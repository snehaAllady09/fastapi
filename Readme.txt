uvicorn main:app --reload

pydantic - useful for data validation

fastapi lo by default we have swagger - http://127.0.0.1:8000/docs

model - represents data
The name "psycopg2" is derived from:
"psyco" - This refers to Psyco, which was a just-in-time (JIT) compiler for Python that was popular in the early 2000s. Psyco was designed to speed up Python code execution.
"pg" - This stands for PostgreSQL (often abbreviated as "Postgres" or "PG").
"2" - This indicates it's the second major version of the library.

